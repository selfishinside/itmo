spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studs
    username: s408742
    password: 8YT1zQmNdKAZ3vZy
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    ru.itmo.love: DEBUG
    org.springframework.transaction: TRACE
    org.springframework.orm.jpa: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

app:
  bitrix:
    # на Helios URL реального Bitrix24 берём из .env
    base-url: ${BITRIX_BASE_URL}
    responsible-id: ${BITRIX_RESPONSIBLE_ID:1}
